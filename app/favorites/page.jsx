import React from 'react';
import FavoritesPage from '../FavoritesPage';

export const metadata = {
  title: 'Blogify | Favorite blogs',
};

const Favorites = () => {
  return <FavoritesPage />;
};

export default Favorites;
